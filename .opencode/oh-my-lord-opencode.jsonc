{
  // ==============================================
  // Native Claude Code Migration Config
  // Branch: feature/native-claude-code-migration
  // ==============================================

  // PHASE 3: Hooks replaced by .claude/hooks/hooks.json
  "disabled_hooks": [
    // Major token savings - these inject AGENTS.md/README.md on every file read
    "directory-agents-injector",
    "directory-readme-injector",

    // Replaced by .claude/hooks/hooks.json + scripts
    "hierarchy-enforcer",
    "tdd-enforcement",
    "planner-md-only",
    "sisyphus-orchestrator",

    // Replaced by native session management
    "todo-continuation-enforcer",
    "compaction-context-injector",

    // Replaced by native keyword routing via agent descriptions
    "keyword-detector"
  ],

  // Keep these hooks - no native equivalent
  // "parallel-safety-enforcer" - file locking
  // "edit-error-recovery" - retry failed edits
  // "background-notification" - useful notifications

  // ==============================================
  // PHASE 4: Tool Migration Notes
  // ==============================================
  // Tools don't have a disable config - they remain available but unused
  // when using native agents.
  //
  // REPLACED BY NATIVE (not used when using .claude/agents/):
  // - delegate_task -> native Task tool
  // - glob -> native Glob tool
  // - grep -> native Grep tool
  // - look-at -> native Read tool
  // - skill -> native Skill tool
  //
  // KEEP AS MCP (valuable, no native equivalent):
  // - lsp - Language Server Protocol diagnostics
  // - ast-grep - AST-based search and refactoring
  // ==============================================

  // PHASE 3: Disable legacy agents (using native .claude/agents/ instead)
  "disabled_agents": [
    "Paul",
    "planner-paul",
    "worker-paul",
    "Sisyphus-Junior",
    "frontend-ui-ux-engineer",
    "Joshua",
    "Peter",
    "John",
    "Solomon",
    "Timothy",
    "Nathan",
    "Elijah"
  ]
}
